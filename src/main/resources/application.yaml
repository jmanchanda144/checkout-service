spring:
  application:
    name: checkout-service
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC


server:
  port: 8082

machine:
  id: 3

razorpay:
  key:
    id: 
    secret:
  webhook:
    secret: 